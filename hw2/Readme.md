# Сервис для извлечения тегов из статей парсера

### Для запуска:
```bash
docker-compose up

# долго проставляет все теги
curl -X POST /extract-tags
```

Проверить полученные теги можно в таблице article_tags в бд